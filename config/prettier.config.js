/**
 * @type {import('./types').PrettierConfig}
 */
const config = {
  singleQuote: false,
  arrowParens: "always",
  semi: false,
  bracketSpacing: true,
  trailingComma: "es5",
  printWidth: 80,
}

module.exports = config
